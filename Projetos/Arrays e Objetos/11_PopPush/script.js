// Observação: POP remove o último elemento do array, e PUSH, adiciona.

let nomes = ["Kevin", "Poly", "Pony", "Gabi"];

let elementoRemovido = (nomes.pop());
console.log(nomes);

nomes.push("Ariel");
console.log(nomes);



